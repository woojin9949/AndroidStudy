val number: Int = 10
println(number)

/*val number1 = 10 * 427 % 73 + 108 //이런경우 값의 타입을 안써도 무방
var number7: Int = 22
println(number7)*/

var number6 = 25
println(number6)

val number10: Int = 1
val number11: Int = 2
val result: Boolean = number10 > number11
println(result)

val result1: Boolean = number10 == number11
println(result1)
// ===, !== 은 잘 사용하지 않는다!!
//  - 주소값이 같은지 물어보는 연산자
//==, !=
//  - 값이 같은지 물어보는 연산자
