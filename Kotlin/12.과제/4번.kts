fun inputToHundred(num: Int) {
    var number: Int = num;
    while (number < 100) {
        number++
        println("1증가!!" + number)
        //if (number == 100) break
    }
    return

}
inputToHundred(90)